const prompt = require('prompt-sync')();

const playlist = ["Música 1", "Música 2", "Música 3","Pagodinho"];

console.log(`Sua playlist atual tem: ${playlist}`);
console.log(`A primeira música da lista é: ${playlist[array-exibicao.js0]}`);

// // Adicionando uma nova música via usuário
let novaMusica = prompt("Qual música deseja adicionar? ");
playlist.push(novaMusica);

console.log(`Playlist atualizada: ${playlist.join(" - ")}`);